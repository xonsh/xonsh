---
model: claude-opus-4-6
service: claude
timestamp: 2026-04-15T00:55:22Z
git_ref: d75d31a9
---

Let me explore the current build system and project configuration.

[... exploration phase with multiple agents reading setup.py, pyproject.toml, setup.cfg, MANIFEST.in, CI workflows, nix configs ...]

Good, so:
1. **Backend**: hatchling
2. **Versioning**: Keep manual `__version__` in `xonsh/__init__.py` for now, drop dirty_version(). Plan for hatch-vcs as a follow-up.
3. **Parser tables**: Build hook in `hatch_build.py`

[... plan mode: comprehensive analysis of build system, design decisions, edge cases ...]

## Implementation

### hatch_build.py (created)

```python
"""Hatch custom build hook for xonsh.

Generate PLY parser tables and set Python-version-specific wheel tags.
"""

import os
import sys

from hatchling.builders.hooks.plugin.interface import BuildHookInterface

TABLES = [
    "xonsh/parser_table.py",
    "xonsh/completion_parser_table.py",
]


def _python_tag():
    """Return the Python version tag, e.g. ``py313``."""
    return f"py{sys.version_info.major}{sys.version_info.minor}"


def _clean_tables(root):
    """Remove generated parser table files."""
    for table in TABLES:
        path = os.path.join(root, table)
        if os.path.isfile(path):
            os.remove(path)
            print(f"Removed {path}")


def _build_tables(root):
    """Generate PLY parser and completion parser tables."""
    print("Building lexer and parser tables.", file=sys.stderr)
    # XONSH_DEBUG=1 is required for parser table generation
    os.environ["XONSH_DEBUG"] = "1"
    sys.path.insert(0, root)
    try:
        from xonsh.parser import Parser
        from xonsh.parsers.completion_context import CompletionContextParser

        Parser(
            yacc_table="parser_table",
            outputdir=os.path.join(root, "xonsh"),
            yacc_debug=True,
        )
        CompletionContextParser(
            yacc_table="completion_parser_table",
            outputdir=os.path.join(root, "xonsh"),
            debug=True,
        )
    finally:
        sys.path.pop(0)


class CustomBuildHook(BuildHookInterface):
    """Custom build hook for xonsh parser tables and wheel tags."""

    PLUGIN_NAME = "custom"

    def initialize(self, version, build_data):
        """Generate parser tables and set wheel tag."""
        root = self.root

        _clean_tables(root)
        _build_tables(root)

        # Force-include the generated table files (they are gitignored)
        for table in TABLES:
            src = os.path.join(root, table)
            if os.path.isfile(src):
                build_data["force_include"][src] = table

        # Set Python-version-specific wheel tag
        if self.target_name == "wheel":
            build_data["tag"] = f"{_python_tag()}-none-any"

    def clean(self, versions):
        """Remove generated parser tables."""
        _clean_tables(self.root)
```

### pyproject.toml (key changes)

Changed `[build-system]` from setuptools to hatchling:
```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```

Replaced all `[tool.setuptools.*]` sections with:
```toml
[tool.hatch.version]
path = "xonsh/__init__.py"

[tool.hatch.build]
artifacts = [
    "xonsh/parser_table.py",
    "xonsh/completion_parser_table.py",
]

[tool.hatch.build.targets.wheel]
packages = ["xonsh", "xontrib", "xompletions"]

[tool.hatch.build.targets.wheel.hooks.custom]

[tool.hatch.build.targets.sdist.hooks.custom]
```

Made readme static with correct content-type:
```toml
dynamic = ["version"]
readme = {file = "README.rst", content-type = "text/x-rst"}
```

Migrated mypy config from setup.cfg to `[tool.mypy]` and `[[tool.mypy.overrides]]`.
Migrated pytest config from setup.cfg to `[tool.pytest.ini_options]`.
Removed `"setup.py"` from ruff `extend-exclude`.

### .github/workflows/publish.yml (modified)

Replaced pip-based build with uv:
```yaml
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: build sdist(tarball) to dist/
        if: ${{ startsWith(matrix.python-version, '3.11') }}
        run: uv build --sdist
      - name: build bdist(wheel) to dist/
        run: uv build --wheel
```

### nix/unwrapped.nix (modified)

Replaced `setuptools` with `hatchling` in function args and `build-system`.

### Deleted files

- `setup.py`
- `setup.cfg`
- `MANIFEST.in`

## Verification results

- `uv build --wheel` → `xonsh-0.22.8-py313-none-any.whl` (correct tag)
- `uv build --sdist` → `xonsh-0.22.8.tar.gz` (contains parser tables)
- Editable install works, `python -m xonsh --no-rc -c 'echo "hello"'` succeeds
- 2817 tests passed (aliases, environ, parsers)
- ruff lint/format clean on `hatch_build.py`
- `lexer_table.py` removed from TABLES (was never generated, vestigial)
