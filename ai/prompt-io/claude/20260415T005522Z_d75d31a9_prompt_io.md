---
model: claude-opus-4-6
service: claude
session: stateful-finding-church
timestamp: 2026-04-15T00:55:22Z
git_ref: d75d31a9
scope: code
substantive: true
raw_file: 20260415T005522Z_d75d31a9_prompt_io.raw.md
---

## Prompt

User asked to port the xonsh repo from the old `setup.py` style to use `uv`
(hatchling as build backend). Three interactive decisions were made:

1. Build backend: hatchling (recommended)
2. Versioning: keep manual `__version__` for now, plan `hatch-vcs` as follow-up
3. Parser tables: build hook at build time (recommended)

## Response summary

Migrate xonsh build system from setuptools + `setup.py` to hatchling + `uv`.
Create a custom hatchling build hook (`hatch_build.py`) that generates PLY
parser tables at build time and sets Python-version-specific wheel tags. Move
all config into `pyproject.toml`, including mypy and pytest settings previously
in `setup.cfg`. Update CI publish workflow to use `uv build`. Update nix config
to reference hatchling. Drop vestigial logic: `dirty_version()` dev suffix,
`dev.githash` generation, custom `install_scripts` shebang rewriting,
never-generated `lexer_table.py`.

## Files changed

- `hatch_build.py` — new: custom hatchling build hook (parser tables + wheel tags)
- `pyproject.toml` — switch build backend, add hatch config, migrate mypy/pytest
- `setup.py` — deleted: replaced by `hatch_build.py` + pyproject.toml
- `setup.cfg` — deleted: config migrated to pyproject.toml
- `MANIFEST.in` — deleted: hatchling uses gitignore + `artifacts` config
- `.github/workflows/publish.yml` — use `uv build` instead of `python -m build`
- `nix/unwrapped.nix` — replace `setuptools` with `hatchling`

## Human edits

None — committed as generated.
